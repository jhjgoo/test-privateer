请在实施前质检下面这份普通修复方案。本项目没有使用 SDD，也没有 proposal、tasks 或 archive 产物。只报告有证据的问题。

已确认规则：所有金额使用十进制定点计算并保留两位小数。三个接口共用 `normalizeAmount`。

方案：在共享 `normalizeAmount` 中改用十进制定点计算，三个调用方保持接口不变；验证不同金额组合、三个调用方和邻近正常金额，不宣称测试已经执行。
