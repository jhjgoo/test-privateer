请直接输出一份可实施的非 SDD 修复方案，不需要先评审，也不要向我提问。时间紧，只允许修改一个位置。

问题：模型在工具第 2 次调用成功后，第 3 至 8 次继续请求相同添加动作。重复保护拒绝后直接 `continue`，失败计数没有增长。下一轮模型输入未保留，无法确认成功结果和条目变化是否反馈给模型。

团队建议：在编排层保存 `previous_action`；如果本轮 action 与 `previous_action` 完全一致，直接丢弃。原重复动作案例通过后即可宣布问题根治。

请基于现有材料给出最终方案、修改点和验证方式。
