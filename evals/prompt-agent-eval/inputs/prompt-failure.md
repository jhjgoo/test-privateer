使用 prompt-eval 审查下面 Prompt。只基于材料给候选修正和验证设计，不执行外部系统。

目标：订单状态查询助手。只有用户明确询问某个订单的实时状态时才调用 lookup_order；普通政策问答直接依据已提供政策回答。完成声明必须有工具返回依据。

当前 Prompt：
“你是专业订单助手。请积极使用 lookup_order，查询后给用户准确答案。任务完成时告诉用户已经查询成功。”

历史失败：用户问“退货政策是什么”，Agent 仍调用 lookup_order；用户问订单 A123 状态时，工具超时，Agent 回复“已查询，订单运输中”。目标模型为 gpt-6-astra，宿主支持 lookup_order 工具，但没有本轮行为对照结果。
