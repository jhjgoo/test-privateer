# 本期修改复核

范围：面向任务的测试 Skill 优化方案。本轮为用户已批准的职责、能力和交付契约修改，不是目标模型迁移。参考 skill-eval 的“保留承重规则、检查冲突、避免单次事故补丁堆积”原则。

## 发现与处理

| 位置 | 原指令证据／问题 | 修改及复核 |
| --- | --- | --- |
| heuristic-testing/SKILL.md；clarifying-test-basis/SKILL.md | “你是测试人员的探索搭档”“当前协作者通常是测试人员”将职称与协作权限混用 | 按任务协作；按实际责任、证据和已有授权判断 |
| clarifying-test-basis/SKILL.md | “首次共同探索……然后等待回答”与用户要求自主分析、局部未知可继续的需求冲突 | 关键歧义等待；自主分析保留假设；未知只阻塞相关部分 |
| modeling-tests-with-ppdcs/SKILL.md | 仅强调返回业务化测试点，可能使下游失去实际模型 | 下游取得模型、覆盖准则与来源；用户展示按需 |
| designing-test-experiments/SKILL.md | “状态只能取一个值”缺少未核实状态，且促使按状态重复拆用例 | 允许记录真实多项缺口，区分未核实与已知未实现；设计完成与执行就绪分别说明 |
| designing-test-automation/SKILL.md | “其他层级进入当前技术栈的实现流程”与本期入口限定冲突 | 接口／页面入口只交付设计，框架接入明确未实现；已有测试执行保留 |
| running-test-sessions/SKILL.md；investigating-findings/SKILL.md | 默认要求当前 Spec，独立路径无法闭合 | 读取并写回调用方选定的独立／项目产物 |
| writing-unit-tests/SKILL.md | 新行为 PASS 一律“不得进入 GREEN”，没有明确行为已存在时的处理 | 区分行为已存在与断言无效，不人为制造 RED |
| challenging-test-designs/SKILL.md | 要求 SFDIPOT 完整测试点、实施前必选准入结论 | 回归工具职责；只在任务要求时判断实施准备程度 |

模式参考：自由度配置不当、重复补丁导致契约冲突。以上问题由原文与用户确认的需求直接对照，不宣称旧规则对某个 GPT 版本普遍过时，也不将跨模型经验当作验证证据。

具体 Diff：本轮仓库相对 8b639b1 的工作区差异，使用 git diff -- README.md docs/guide.md skills 查看完整修改。修改已经用户授权并应用；本报告不提供额外待应用规则。

## 验证与限制

- 18 个 Skill 的 YAML 名称和描述有效；Skill 数量未增加。
- 修改涉及的 12 个 Skill／引用文件与本地 .agents 安装副本逐字一致。
- git diff --check 通过；接口／页面无新增实现、框架或脚本。
- 按已确认方案复核目标澄清、局部未知、模型交接、任务语言、自动化入口与执行边界。
- 没有重新运行同模型前后行为对照，没有运行产品测试。静态契约一致性不等于真实会话效果；不能据此宣布 skill-eval 的完整行为验证已通过。真实使用场景按主方案第 8 节继续验收。

## 模板与真实样板迁移复核

后续用户确认了文档拆分、稳定命名、风险生命周期和连续推进，已落实到主入口、产物路由、澄清、分析、设计、风险及其消费者。新增四份模板，不新增 Skill；教程旧单文件布局已更新。

样板为 maycur-ai-audit 对接配置测试资产。测试分析由原混合文档 670 行整理为约 255 行，全部 72 个 TCON ID 保留；16 个 CASE-CORE 用例迁入独立文件，旧 EX 仅作主题索引。已确认规则退出未决项，P0 混用标签不再作为当前执行排序。

迁移前 test.md 与 TCO.md 逐字备份，原 test.md 改为跳转入口。当前产物的本地文件链接已核对存在，未执行任何产品测试或业务接口。项目风险尚未获得目标运行验证的，保留待调查／处理中，未误关闭。

18 个 Skill frontmatter 有效；包含 4 份模板在内的 24 个 Markdown 文件与本地安装逐字一致。模板结构和编号完整性已检查，真实交互是否减少等待仍需后续试用验证，不声称模型行为已得到证明。
