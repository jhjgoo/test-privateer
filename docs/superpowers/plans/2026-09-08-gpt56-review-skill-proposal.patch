--- a/skills/challenging-test-designs/SKILL.md
+++ b/skills/challenging-test-designs/SKILL.md
@@ -1,11 +1,27 @@
 ---
 name: challenging-test-designs
-description: 用于已有测试分析、风险、模型、章程、用例集或完成声明需要在执行或采信前审查时。
+description: 用于测试产物交付前自校与修正，或用户要求审查已有测试资产和完成声明时。
 ---
 
 # 审查测试设计
 
-只报告会改变执行、覆盖或信心的缺陷。风格偏好不是 finding。
+复核测试产物的自洽性与证据链，把有依据的文档缺陷消解在交付前。审查聚焦会改变执行、覆盖或信心的问题。
+
+## 交付自校
+
+生成或实质更新完整测试分析、独立模型或用例集时，负责成稿的 Agent 自行复核并修正本轮产物；上层组装尚未完成的中间结果由组装者统一处理。简短澄清、进度和不改变测试含义的文字调整不启动整套复核。用户只要求审查已有文件时只报告；明确要求修改时才修改这些既有文件。
+
+先固定当前稿与有效依据，逐项核对本轮定义、引用和规则主张。每个引用能定位到唯一且语义相符的定义，缩写可无歧义还原；每个状态模型使用明确主体和身份判定；结论、模型、测试点与当前安排在同一对象、范围和阶段下相符。已有规则支持的检查有去向，宣称完成的更新能在实际产物中找到。编号只是定位手段，核对需阅读其对应内容，不依赖固定 Markdown 版式或编号检查脚本。
+
+发现缺陷后，成稿 Agent 查原文和可取得的依据，自行决定修正。错号、失效引用、遗漏的既定内容和未同步的图表直接修正并更新关联位置；缺号先判断是引用写错还是实际少了内容，不为凑齐编号新增测试。语义冲突先区分主体、适用范围、历史版本、实现事实和目标规则；一般描述与具体规则可兼容时补清适用范围，不推翻有效规则。
+
+涉及多份资产、状态或决策模型、重要规则变化时，在能力允许的情况下使用独立上下文审阅，提供原任务、有效规则、当前稿及必要依据。审阅者返回证据与意见，不代替成稿 Agent 裁决；已被委派的审阅者直接工作，不再委派。独立审阅不可用时由成稿 Agent 完成自校，如实说明验证方式。
+
+成稿 Agent 根据证据接受、驳回或修正复核意见，无需用户逐项批准文档纠错。修改后重新阅读受影响的定义、引用、模型和结论；仍有可依据现有材料解决的缺陷就继续修正，不把它转成用户待办。没有新证据的重复意见由 Agent 对齐范围后裁决，不反复发起全量审查；范围外建议不阻塞当前交付。
+
+只有现有材料和可自行调查的事实仍不能确定业务规则，或需要用户选择范围、接受风险或扩大授权时，才提出具体决策问题。无法确认的业务预期保留为未知，其他已明确内容继续完成。工具失败或修复尚未完成时报告实际未完成项，不包装成需要用户确认的业务问题或复核通过。
+
+交付的是修正后的产物，附必要的修改摘要和真正待决的业务问题；内部审查分歧按需保留证据，不要求用户逐条审阅。自校完成以当前产物的已发现文档缺陷已修正、关联内容已复读且有效规则未被误改为准；有未知时范围和影响明确。复核不等于产品测试执行或通过。
 
 ## 检查分析链
 
@@ -62,7 +78,7 @@
 能够修复断裂的一项可检查改动
 ```
 
-按影响排序，保留正确内容。只有用户要求时才重写资产。
+独立审阅按上述格式把意见返回成稿 Agent；用户只要求审查时直接交付审查结果。成稿交付按“交付自校”修正并简述结果，保留正确内容；不以罗列审查发现代替完成修正。
 
 表格不是目的。只有表达方式妨碍比较、执行或审查时，才把它报告为 finding；单条复杂 Case 使用详细步骤不是缺陷。
 
@@ -71,4 +87,4 @@
 - **足以支持实施：** 实施计划无需发明产品行为、规则、边界或 oracle。
 - **暂不足以支持实施：** 列出阻塞未知、证据来源、应答角色及其测试影响。
 
-**完成条件：** 每个 finding 都有直接证据、实际影响和一个最小可验证修正；项目基线、模型、TCON 粒度和实施前门槛已按适用范围检查；没有把个人偏好包装成缺陷。
+**完成条件：** 每个审查发现都有直接证据、实际影响和最小修正；项目基线、模型、TCON 粒度和阶段边界已按范围检查。成稿任务满足“交付自校”的完成条件；仅审查任务返回有依据的意见与核实限制。
--- a/skills/analyzing-change-for-testing/SKILL.md
+++ b/skills/analyzing-change-for-testing/SKILL.md
@@ -83,4 +83,6 @@
 
 只有跨 Spec 或长期有效的认知才升级到 TCO。
 
+完整产物交付前，按 `challenging-test-designs` 的“交付自校”自行复核、修正并检查关联内容；供上层组装的中间结果由组装者统一处理。
+
 **完成条件：** 原请求范围内已识别测试点、风险和未决项均有明确安排，未被专项替代；测试条件可追溯到项目地图或实际依据；产物遵循已读取模板和命名规则，停在请求阶段；需要维护的 TCO 已更新或说明具体缺口；用户能看出先做什么、为什么及下一产物。
--- a/skills/modeling-tests-with-ppdcs/SKILL.md
+++ b/skills/modeling-tests-with-ppdcs/SKILL.md
@@ -30,6 +30,8 @@
 | Combination | 因素、水平、约束、交互强度和可行组合 | 生成组合 |
 | State | 主体、状态、事件、守卫、输出及合法／非法迁移 | 迁移及序列 |
 
+State 模型先明确主体及其身份判定。重建、换属或重新占用涉及新主体时，分别表达旧主体和新主体的迁移；模型与 TCON 沿用同一身份判定。
+
 优先用 Process、Parameter、Data 或 State 理解行为。只有因素、水平和约束已经明确，且需要压缩组合覆盖时才用 Combination。没有标签的箭头、没有取值的参数清单和模型名称都不算模型。
 
 ## 派生 TCON
@@ -44,4 +46,6 @@
 
 向调用方交付实际模型、覆盖准则、排除项、派生测试条件及来源、新问题和未知，供后续设计使用。每个测试点用角色、业务对象、动作和可观察结果命名。用户侧的展示深度按任务选择：点名建模或需要评审时展示完整模型；其他场景突出业务测试点，必要模型与追溯仍须可访问。调用方决定持久化、选择状态和是否具体化。用户只要求 PPDCS 或测试建模时，到此停止。
 
+完整产物交付前，按 `challenging-test-designs` 的“交付自校”自行复核、修正并检查关联内容；供上层组装的中间结果由组装者统一处理。
+
 **完成条件：** 主模型是可检查的实际模型；每个 TCON 可追溯到路径、规则、分区、组合或迁移；粒度差异已拆开；第二模型如存在，新增覆盖明确；TCON 没有冒充可执行测试。
--- a/skills/designing-test-experiments/SKILL.md
+++ b/skills/designing-test-experiments/SKILL.md
@@ -92,4 +92,6 @@
 
 交付当前范围的用例、任务或章程及依据，分别说明设计完成情况、规则缺口和已核实的执行条件。下一步点名编写什么、准备什么或执行什么；未核实版本和环境时不能把用例草案称为可执行。调用方按已有请求决定持久化与执行。
 
+完整产物交付前，按 `challenging-test-designs` 的“交付自校”自行复核、修正并检查关联内容；供上层组装的中间结果由组装者统一处理。
+
 **完成条件：** 所有本轮选中内容均已列入覆盖边界；常规 Case 可快速扫描，复杂 Case 有必要细节；每条 Case 状态诚实、前置独立、证据最小，并可追溯到候选点或 TCON；P0 与产品价值和核心证据一致；未执行内容没有冒充结果。
