# 未提交实现审查与 GPT-5.6 Skill 审计

审查范围：当前 18 个已跟踪文件的未提交 diff，以及必要的主入口、交付模板和相邻规则。只读审查仓库；本审查者仅写入本临时报告及空 proposed diff。未审查与本实现无关的历史研究及提案。目标模型为用户指定的 GPT-5.6。

结论：发现的 2 项指南追溯缺陷已由成稿 Agent 修正并经本审查者复读验证；目前没有需继续修正的 finding。未发现有目标证据支持的 GPT-5.6 过时提示模式，Group 1–4 均为 0 项；模型审计 proposed diff 为空。

## 已修正的审查发现

1. **原 P2：教学执行记录引用不存在的旧用例 ID。**
   - 位置：`docs/guide.md:290`，定义位于 `docs/guide.md:270`。
   - 证据：原句为“下面是假设执行 C-02 后得到的教学记录”，用例已改为 C-002。
   - 影响：读者无法沿当前定义直接找到执行记录对应的用例；此处是教学假设记录，不是真实历史证据保留的例外。
   - 最小修正：同步为 C-002，或明确建立旧号到新号映射。
   - 验证：已读取修正后的定义与引用，均为 C-002，记录的关闭 E1 后旧页面确认行为与定义对应。

2. **原 P2：T-002 细化及用例转换丢失“未确认不写入”。**
   - 位置：`docs/guide.md:238`、`docs/guide.md:269`，原定义位于 `docs/guide.md:187`。
   - 证据：初始 T-002 包括“操作前后选中目标一致，未确认不写入”；原细化仅描述有效候选正确关联，原 C-001 只要求选择后确认，关联安排又将 T-002 归入该用例。
   - 影响：沿用同一 ID 时静默缩小对象语义，且已知规则没有待设计去向，可能把确认前提前写入的缺陷漏掉。
   - 最小修正：细化行保留未确认不写入及确认目标一致；C-001 添加确认前观察及预期，或将独立行为拆出并保留明确去向。
   - 验证：现 T-002 明确“未确认不写入；确认后关联目标与用户选中的有效候选一致”；现 C-001 在确认前检查关联，并有对应不写入预期。原观察点已保留。

## 其余语义与入口核对

- 编号权威规则在 artifact-routing，N/R/T/C 类型与项目、需求范围明确；MFQ 类型不进入 ID；方法入口引用该规则，没有另一份冲突的分配规则。
- 负责合成的 Agent 定号，子 Agent 复用已给编号并交回新增项；候选细化与真实拆分区分，合并、撤回和跨范围升级保留映射。
- 已核对指南当前 T-001 至 T-009 及 C-001 至 C-004 的定义和实际关联。T-007 的拆分去向、T-008/T-009 来源、待设计项及用例的多对多关系一致，没有剩余旧 C-02 引用。
- 完整需求分析、独立 PPDCS 模型及用例集入口均明确路由交付自校；上层尚未组装的中间产物留给组装者处理，独立审阅不再委派。
- 只审查时只报告的边界明确；自动修正限定本轮成稿与已有修改授权。未知业务规则、接受风险及范围/授权决策仍交由用户，未自动授权产品执行或修改原始历史证据。
- 用例更新回填主分析的关联与安排；依据不独立维护测试项或风险状态。历史执行记录、外部权威编号与范围外引用保留原身份，通过映射连接。
- 没有编号脚本或固定 Markdown 解析器要求。现有图表及模板是文档呈现约定，不是机器解析前提。

## GPT-5.6 审计方法与证据

已读取 `/Users/jianghongjian/.agents/skills/skill-eval/SKILL.md`、完整 `shared/prompt-audit.md`、`references/target-model-evidence.md`、`references/openai-gpt-5.6.md`，并检查 `shared/prompt-caching.md` 的静默失效模式。此次没有 API 模型迁移或请求配置修改，因此 Claude 模型迁移清单不作为 GPT 证据或修改依据。

目标证据是用户于 2026-09-03 提供的《Using GPT-5.6》本地摘录，来源期望地址为 `https://developers.openai.com/api/docs/guides/model-guidance?model=gpt-5.6`。这是快照，不是本次实时核实的 API 契约。

提示面清单：修改的 11 个 SKILL.md、5 个 references 文件，以及 README、guide 中的使用指导和示例；为确定路由，读取 heuristic-testing 主入口及其 openai.yaml。仓库是 Markdown Skill 包，清单中没有请求构造代码、模型调用循环、工具 schema 或专职 agent roster。openai.yaml 仅设置主入口的隐式调用策略。

执行了以下静态核对：

- 对新增交付自校及编号规则执行 git blame：它们属于本轮未提交新增，邻接规则来自 2026-09-04/07，没有可归因为旧 Claude 模型的迁移来源。
- 对 skills、README 和 guide 扫描压力词、旧模型、思考脚手架、数值输出限制、重复提示、API 参数和缓存失效信号；逐项区分业务约束、路由和旧提示候选。
- 对缓存相关时间戳、UUID、随机数、序列化、动态 system/tool 构造及模型调用站点执行搜索：没有运行时代码命中；不提出 Claude 缓存 API 替换。
- 检查独立审阅及子 Agent 规则：承担核对与主稿裁决的不同职责，并限制重复审查和继续委派，不是相同任务的冗余角色清单。
- 检查旧编号字符串及对应定义；修复后复读语义，不以正则存在性代替内容验证。
- `git diff --check` 退出码 0。

保留判断：GPT-5.6 快照要求保留领域背景、硬约束、授权边界和成功标准。本轮新增身份判定、证据保护、未知处理、编号映射、复读及停止条件均表达用户已批准的交付契约；不能仅因措辞详细或包含禁止语就认定过时。各入口的一行路由是按需加载的连接，不是冲突的规则副本。现有风险分析的 1～5 项属于相邻的本轮产物约定；没有本工作负载中造成截断的目标模型证据，不提出删改。

验证限制：本次为源码/文档静态审计，没有 GPT-5.6 运行端点或请求日志，未执行目标模型前后行为实验，不能声称实测了性能、成本或稳定性改善。没有提出提示删除或 API 变更，因此不存在待验证的 contested cut。正常的语义纠错通过源定义—模型—用例—安排的实际内容对照验证。

## Proposed diff

GPT-5.6 模型审计无可支持的删除或改写项，`/tmp/testing-skills-implementation-review.patch` 为 0 字节空 diff。以上两项语义缺陷已经由成稿 Agent 修正，本审查者没有改动仓库文件。


## 后续限定复核：用例关联回填的交付条件

仅复核 `skills/designing-test-experiments/SKILL.md:14`、`:95`、`:99` 的后续变化及直接相关的授权/组装约定，没有重新展开其他范围。

结论：无新增 finding。将“同步主分析”从输入位置移至交付和完成条件，使回填成为可核实的成稿结果；实际 C 编号与当前安排逐项写入来源 T，只有文件链接不算完成，符合 artifact-routing:42 已有规则。

授权及职责核对：

- 同段仍保留“调用方按已有请求决定持久化与执行”；“可维护的主分析”受 artifact-routing:3 的当前请求范围及 :44 的有权修改文件集合约束，不构成修改任意可访问文件的额外授权。
- 来源明确只读或不可访问时，交付逐项映射并诚实说明未回写，保留了只读约束和证据边界；没有把具备读取能力等同修改授权，也没有迫使用户先解除限制。
- 回填责任明确落在成稿 Agent；下一段仍规定尚未组装的中间结果由上层组装者统一处理，与子 Agent 只提供新内容和组装者统一定号一致。
- 仅审查任务依旧服从 challenging-test-designs:12，只报告问题；该改动针对已获授权的用例生成或更新交付，没有扩大为审查时自动修改源文件。
- “已回填或如实说明未回写”与正文要求的逐项映射合读，不等于可省略回填，也不允许未回写却声称同步完成。

GPT-5.6 依据：成稿 Agent 说明此次调整针对 fresh GPT-5.6 试跑中“用例文件有映射但主分析未回填”的实际失败。本审查者未读取该试跑 transcript，因此仅将其记录为成稿 Agent 提供的修正动因；不宣称本审查者完成了前后行为验证。新文字表达可检查的结果与真实授权边界，符合已有 GPT-5.6 快照要求保留成功标准和硬约束的解释，没有需另提的过时提示删改项。

验证：复读改动全文及 artifact-routing:3、:34–44、challenging-test-designs:12–24；限定 `git diff --check -- skills/designing-test-experiments/SKILL.md` 退出码 0。仓库文件未由本审查者修改，proposed diff 继续为空。
