# GPT-5.6 交付复核只读试跑原始报告

日期：2026-09-08。以下是两位独立审阅者的输出，包含主审阅者尚未采纳的意见，不是已确认缺陷清单。解释与最终方案见 docs/superpowers/plans/2026-09-08-gpt56-review-skill-proposal.md。两个执行者均指定 gpt-5.6-sol / medium，各一次，未获得既有缺陷答案；原文件未修改。

---

## 原版审查Skill

# 测试资产独立审阅报告

## 审阅范围与判定

本次仅审阅以下两份产物，不执行其中提到的命令，不核验源码、PRD、OpenSpec、测试文件或历史验证记录，也不形成产品 Finding 或发布批准：

- `/Users/jianghongjian/Downloads/5.6/TCO.md`
- `/Users/jianghongjian/Downloads/5.6/2026-09-05-integration-config-test-analysis.md`

两份产物已经覆盖项目业务链、证据边界、需求风险、模型、候选测试条件和残余未知，整体上完成了“形成 TCO 与候选测试内容”的原任务。以下 4 项会影响后续测试设计、覆盖追溯或判定，应在把分析转成测试任务／用例前修正。

## Finding 1：缺少可追溯决策的关键安全变更已被用作有效 oracle

断裂点 — 需求分析第 61～63 行把“不新加密”和“可切换真实 Secret”作为当前测试 oracle；第 66 行又明确承认 OpenSpec 只写“用户已确认”，没有链接独立确认记录。随后第 263～264 行按该规则设计凭证落库和接口暴露测试，第 281～285 行把相关代码及测试资产列入“证据支持的主张”。默认地址变更也存在相同来源问题（第 59、66 行），但安全行为的判定后果更严重。

## 影响

后续测试可能把“明文保存／授权角色可查看真实 Secret”判为正确，而原 PRD 据同一资产记载要求加密和脱敏。缺少可审计的规则决策时，这会直接反转安全测试的通过／失败判定；源码或现有测试只能证明当前行为，不能证明该行为就是有效需求。

## 最小修正

在第 61～63 行所依据的 OpenSpec 规则旁补一个可核验的需求决策来源（决策记录、负责人、日期和适用版本）；补证前把这些 oracle 标为“待确认”，把当前实现改记为观察事实，并保留“加密／不加密、脱敏／可回显”两条候选测试分支，不给出通过判定。

## Finding 2：项目基线与需求分析对快照固定时点给出不同规则

断裂点 — `TCO.md` 第 13 行把 `AuditTask` 的 JSON 快照定义为固定“任务创建或执行时”的业务依据；需求分析第 36、46、63、147、224～226、283 行则一致要求对接配置在“任务创建时”固定，并把创建时点作为编辑不回溯和新旧任务对照的 oracle。

## 影响

测试设计者若使用项目 TCO，会接受执行时才取快照；若使用需求分析，则会在创建后、执行前修改配置并期待旧任务仍使用旧值。两种口径会让同一观察得到相反判定，也会影响并发边界、历史展示和回写模式测试。

## 最小修正

把 `TCO.md` 第 13 行改为按快照类型区分固定点，并明确“本需求的对接配置快照在任务创建时固定”；如果其他 AuditTask 快照确实在执行时固定，分别命名并列出，不再用“创建或执行时”概括成单一规则。

## Finding 3：部分 TCON 混合了不同角色、状态和未知 oracle

断裂点 — 需求分析第 264 行的 `TCON-SEC-02` 把 tenant admin、platform admin、reviewer、未登录四种权限角色放在一个条件中，只写“设计允许的字段”，但第 33、169～172 行已经表明这些角色的菜单、读写权限和可见摘要不同。第 241 行的 `TCON-WB-10` 又把投递成功、待重试、失败以及人工复核并发放在一个条件中；同一行只明确“已回写不重复”，同时承认失败恢复规则尚未知。第 228～229、312 行也说明在途恢复 oracle 尚待确认。

## 影响

这些 TCON 不能稳定派生测试任务：不同角色需要不同权限和字段 oracle，不同投递状态需要不同前置数据、动作、观察位置及恢复结果。继续按单个 TCON 追踪时，某一已覆盖分支容易被误认为整项已覆盖，未知恢复策略也可能藏在已知的“不重复”结论里。

## 最小修正

为 `TCON-SEC-02` 按角色和接口动作建立可独立追踪的子条件，并写出各自允许字段／拒绝结果；为 `TCON-WB-10` 至少按“投递成功、待重试、终态失败”拆分，成功分支保留现有 oracle，另外两支明确标为受第 312 行规则确认阻塞。

## Finding 4：风险到测试条件的编号引用包含不存在或不完整的目标

断裂点 — 需求分析第 118 行的 R-IC-01 引用 `HIS-03`，全文没有该编号；第 120 行的 R-IC-03 引用 `TCON-SEC-01～06`，实际仅定义到 `TCON-SEC-04`（第 263～266 行）；第 125 行的 R-IC-08 引用 `TCON-LOG-01～04`，实际仅定义到 `TCON-LOG-02`（第 267～268 行）。第 119、124 行还分别使用 `WB-10`、`WB-08、09` 的缩写，而正式编号为 `TCON-WB-*`。

## 影响

风险覆盖统计会把不存在的条件误算为已规划，审阅者也无法判断 SEC-05/06、LOG-03/04、HIS-03 是遗漏内容、旧编号还是尚未设计的测试点。执行准备阶段无法可靠区分“待转换、阻塞、排除和漏项”。

## 最小修正

逐项校正风险表的关联测试列：删除失效编号或补齐实际缺失的具名 TCON，并统一使用完整 `TCON-*` 编号；完成后做一次“风险表中的每个编号都能在测试点清单中唯一命中”的机械校验。

## 证据限制与下一步

本审阅无法独立验证两份产物关于分支 SHA、源码结构、迁移机制、PRD、OpenSpec、现有自动化及 `verify.md` 的陈述，因为这些来源不在指定审阅证据内。因此，本报告不判断 R-IC-01 等产品风险是否真实发生，只判断两份测试资产内部能否支持一致、可追溯的后续测试设计。

下一步应先修正上述 4 项资产断点，再把高优先级候选条件转换为测试任务或用例；产品规则仍未知的分支继续保留为阻塞项，不应提前写成可执行 oracle。

---

## 候选v1审查Skill

# 测试资产独立审阅报告

## 已检查范围与版本

- 原任务范围：项目任务的测试分析，形成 TCO 与候选测试内容；不执行产品测试，不要求直接生成可执行用例，不作发布批准。
- 审查方法：依据 `challenging-test-designs` 检查稳定 ID、跨文件引用，以及测试对象 → 产品内容 → 风险 → 模型 → TCON → 证据与结论的分析链。
- 固定产物：
  - `/Users/jianghongjian/Downloads/5.6/TCO.md`，170 行；文内自述观察版本为 `release/20260910` 的 `f856af46`。
  - `/Users/jianghongjian/Downloads/5.6/2026-09-05-integration-config-test-analysis.md`，328 行；文内自述目标 feature 为 `f10bf001`，当前观察版本为 `f856af46`。
- 本次为只读文档审查；未执行产物中的命令，未运行产品测试，未修改原文件。

## Finding 1（高）：风险到测试条件的稳定 ID 引用存在缺号、错写和不可拆分的范围定义

### 断裂点与直接证据

- 测试分析第 118 行，`R-IC-01` 关联 `TCON-MIG-01、02、HIS-03`；全文没有 `HIS-03` 定义。
- 第 119 行，`R-IC-02` 关联 `TCON-SNAP-05、06、WB-10`；实际定义是第 241 行的 `TCON-WB-10`，`WB-10` 不是已定义 ID。
- 第 120 行，`R-IC-03` 关联 `TCON-SEC-01～06`；第 263–266 行只定义了 `TCON-SEC-01～04`，`05、06` 不存在。
- 第 124 行，`R-IC-07` 关联 `WB-08、09`；实际定义是第 239–240 行的 `TCON-WB-08、09`。
- 第 125 行，`R-IC-08` 关联 `TCON-LOG-01～04`；第 267–268 行只定义了 `TCON-LOG-01～02`。
- 第 237 行把 `TCON-WB-01～06` 合并成一行，但没有说明 01–06 分别对应决策表第 89–92 行中的哪一个组合；这些 ID 因而不能单独追溯到具体模式、AI 结果和 oracle。

## 影响

风险选测、后续转用例和覆盖汇总会出现两类错误：引用不存在的测试条件会造成虚假覆盖；把六个 ID 合并为未映射的范围，会使执行者无法确认某一格是否已转换、已覆盖或仍阻塞。`R-IC-03` 和 `R-IC-08` 还会分别把不存在的两个安全条件、两个日志条件计入范围。

## 最小修正

逐项修正风险表引用：删除或补定义 `HIS-03`、`TCON-SEC-05/06`、`TCON-LOG-03/04`，把 `WB-08/09/10` 统一为 `TCON-WB-08/09/10`。为 `TCON-WB-01` 至 `06` 增加一张六行映射，明确每个 ID 的 `模式 × AI 结果 × approveResult`；若无需独立跟踪，则只保留一个矩阵 ID，避免伪造六个稳定 ID。

## Finding 2（高）：在途回写所用 requestUrl 在同一资产中同时被画成既定规则和待确认规则

### 断裂点与直接证据

- 测试分析第 46 行把 requestUrl 纳入任务创建时的非凭证快照；第 48 行表示按快照模式构造投递。
- 第 49 行又把“当前凭证与地址”直接输入 `WebhookDelivery`，第 51 行将编辑或解绑描述为影响“后续任务和当前调用”，图中没有未知或条件标记。
- 第 228 行的 `TCON-SNAP-05` 却把“凭证/地址变化”后的处理明确标为 `oracle 待确认`；第 13 行摘要也说需要明确编辑或解绑后是继续、可恢复失败还是永久停止。
- 第 36 行只明确配置变化不改变“历史展示与模式”，没有明确旧任务实际外呼应使用快照地址还是当前地址。

## 影响

数据流图会让后续设计者把“使用当前地址”当作已确认行为，而 TCON 与摘要又要求先确认。两种理解会产生相反的测试 oracle：配置编辑后向新地址投递可能在一套测试中判为正确，在另一套测试中判为错投；解绑后的恢复设计也会随之改变。

## 最小修正

在数据流图中把地址从已确定的 `C --> D` 边移入“待确认的在途回写策略”，并将凭证编辑、地址编辑、编码修改、解绑拆成四个待确认事件。只保留已确认事实：快照保存 requestUrl 用于历史展示、模式按创建时快照；在确认前不要声明实际外呼使用当前地址或快照地址。

## Finding 3（中）：项目 TCO 对快照固定时点保留了已被需求分析废止的二选一表述

### 断裂点与直接证据

- `TCO.md` 第 13 行定义 `AuditTask` 时写的是“JSON 快照固定任务创建或执行时的业务依据”。
- 测试分析第 63 行明确记录：原 PRD 的“创建或执行时”已由当前 OpenSpec 收敛为“任务创建时固定”，本分析 oracle 使用创建时快照；第 36 行和 `TCON-SNAP-01`（第 224 行）也都采用创建时边界。
- `TCO.md` 第 43 行、第 119 行虽描述受理后固定快照，但没有消除第 13 行的“或执行时”。

## 影响

项目级基线仍允许在执行时读取变化后的配置，而需求级 TCON 要求创建时固定。后续跨 Spec 分析若只从 TCO 取规则，可能接受创建后、执行前的配置漂移，削弱 `Q-04` 和 `F-04` 的快照保护，并与本需求的测试结论相反。

## 最小修正

把 `TCO.md` 第 13 行改为“JSON 快照在各入口统一创建 AuditTask 时固定当次业务依据”；如果项目内确有其他快照在执行时固定，应分别命名快照主体并说明各自边界，不能继续用同一句二选一覆盖。

## 未核实部分与证据限制

- 两份产物引用的 PRD、`design.md`、`verify.md`、`tasks.md`、源码、migration、测试文件和测试原始日志不在本次允许依据内。因此，关于具体实现结构、当前 SHA 内容、历史测试覆盖、迁移查询条件、UI 实际列、Node 版本和真实接口行为的主张，本次均未独立核实；这里只检查它们在两份产物中的表达与互相一致性。
- `TCO.md` 的项目证据索引和需求分析的“关联产物与依据”提供了来源名称，但两份交付物自身没有附带可审查的证据摘录。现有文档已经多次把代码观察与执行证据区分开，也明确拒绝“当前 release 已通过全部验收”的主张，因此本次不把来源未提供单独判为产品或测试失败。
- 未发现把候选测试条件冒充已执行用例、把测试设计写成产品通过证据，或作出发布批准的情况。文档明确说明 TCON 不是已执行用例，信心分级也总体与其声明的证据限制一致。

## 当前请求完成程度

两份资产已覆盖项目级业务链、主要交互节点、需求风险、模型、候选 TCON、证据强弱和下一动作，基本满足“形成 TCO 与候选测试内容”的原任务范围。上述 3 项修正完成前，风险到测试条件的覆盖汇总不可可靠机械使用，且在途地址与快照时点可能导致相反判定；因此建议将当前版本标记为“分析内容已形成，引用与两项规则表达待修正”，不解释为测试已执行或产品已验收。

---

## 候选v1差异存档

以下不是最终建议patch，仅用于确定试跑输入。

```diff
--- a/skills/challenging-test-designs/SKILL.md
+++ b/skills/challenging-test-designs/SKILL.md
@@ -1,11 +1,24 @@
 ---
 name: challenging-test-designs
-description: 用于已有测试分析、风险、模型、章程、用例集或完成声明需要在执行或采信前审查时。
+description: 用于完整测试分析、模型或用例集交付前复核，或用户要求审查已有测试资产与完成声明时。
 ---
 
 # 审查测试设计
 
 只报告会改变执行、覆盖或信心的缺陷。风格偏好不是 finding。
+
+## 调用与复核边界
+
+完整测试分析、独立模型或用例集准备交付时，由本轮产物组装者统一复核；用于上层组装的中间结果先返回，不逐层重复审查。简短澄清、进度回复和不改变测试含义的文字修正不触发完整复核。用户明确只要未复核草稿时保留该交付状态。
+
+组装者固定本轮产物版本，提供原任务、当前阶段、已确认范围与规则、产物和必要依据。支持独立上下文时，委派一个只读审查者，按实际可用工具合同调用；不提供作者的自我辩护或预设缺陷答案。已被委派的审查者直接完成审查，不再委派。无法独立审查时可以自检，并明确记录独立复核未执行。
+
+先核对稳定 ID 的定义与引用、范围缩写和重复定义。优先使用已有确定性检查工具；没有工具时逐项核对，不在审查中搭建检查框架。跨文件引用需要相应定义文件；语法含糊、文件缺失或来源不可访问时标为未核实，不能按缺号或通过处理。结构正确后仍须检查下述分析链。
+
+一次完整审查后，审查者把直接证据、影响和最小修正返回组装者。原作者核实意见，在原任务授权范围内修正产物并同步关联内容；来源不足或需要改变业务规则的意见保留待决，不为消除意见发明预期。审查者只核对修正项及其直接影响，不重新扩大范围；局部核对后仍未解决的问题随草案交付，不启动无限复审。
+
+复核结论绑定实际检查的产物版本；会改变规则、模型、测试条件或证据主张的后续修改需要补查受影响内容。没有发现问题时如实返回，不要求凑数。复核完成只说明本轮审查已执行，不表示产品测试通过或已获执行、发布授权。
+
 
 ## 检查分析链
 
@@ -25,7 +38,7 @@
 - 风险脱离产品内容，或没有改变测试；
 - 多个候选测试点未经用户选择、产品风险或新证据筛选，就直接挑熟悉的技术问题建模；
 - PPDCS 只有模型标签，没有实际模型、覆盖准则和 TCON；
-- TCON 无法追溯到路径、规则、分区、组合或迁移；
+- 风险、模型、TCON 或 Case 的引用没有对应定义，范围缩写包含未定义编号，或同一 ID 对应多个不同对象；编号存在但不能追溯到具体行为、模型元素或依据；
 - 前置状态、角色、数据分区、动作、观察位置、oracle、关联风险、测试层级或环境不同，却被塞进同一个 TCON；
 - 测试任务缺少前置状态、角色、权限、数据、动作、观察位置或 oracle；
 - Case 集没有说明已转换、待转换、阻塞和排除范围；
@@ -35,7 +48,7 @@
 - 所选数据与观察无法区分正确行为和所防范的错误行为，未满足 designing-test-experiments 的区分力要求；
 - 所有 Case 被要求保存同一套重型证据，或并发重复次数没有依据和停止条件；
 - 未知政策被写成预期结果；
-- 同一规则在结论、模型和测试点中的动作或结果不一致，可能使测试判定相反；
+- 状态图没有明确主体及其身份，迁移中把原对象与新建对象混为一体；同一规则在结论、模型和测试点中的动作或结果不一致，可能使测试判定相反；
 - 用例设计完成被称为可执行，或未核实条件被误报为未实现；
 - 下一步没有说明具体对象、动作和产物，导致用户不知是在设计、准备还是执行；
 - 接口／页面自动化设计入口声称已提供尚未接入的代码实现能力；
@@ -62,7 +75,7 @@
 能够修复断裂的一项可检查改动
 ```
 
-按影响排序，保留正确内容。只有用户要求时才重写资产。
+按影响排序，保留正确内容。返回已检查的范围与版本、审查发现及未核实部分；范围外建议单列。审查者不重写资产，修正由原作者按“调用与复核边界”处理；用户只要求审查时仅交付审查结果。
 
 表格不是目的。只有表达方式妨碍比较、执行或审查时，才把它报告为 finding；单条复杂 Case 使用详细步骤不是缺陷。
 
--- a/skills/analyzing-change-for-testing/SKILL.md
+++ b/skills/analyzing-change-for-testing/SKILL.md
@@ -83,4 +83,6 @@
 
 只有跨 Spec 或长期有效的认知才升级到 TCO。
 
+完整产物交付前，按 `challenging-test-designs` 的“调用与复核边界”处理；供上层组装的中间结果由组装者统一复核。
+
 **完成条件：** 原请求范围内已识别测试点、风险和未决项均有明确安排，未被专项替代；测试条件可追溯到项目地图或实际依据；产物遵循已读取模板和命名规则，停在请求阶段；需要维护的 TCO 已更新或说明具体缺口；用户能看出先做什么、为什么及下一产物。
--- a/skills/modeling-tests-with-ppdcs/SKILL.md
+++ b/skills/modeling-tests-with-ppdcs/SKILL.md
@@ -44,4 +44,6 @@
 
 向调用方交付实际模型、覆盖准则、排除项、派生测试条件及来源、新问题和未知，供后续设计使用。每个测试点用角色、业务对象、动作和可观察结果命名。用户侧的展示深度按任务选择：点名建模或需要评审时展示完整模型；其他场景突出业务测试点，必要模型与追溯仍须可访问。调用方决定持久化、选择状态和是否具体化。用户只要求 PPDCS 或测试建模时，到此停止。
 
+完整产物交付前，按 `challenging-test-designs` 的“调用与复核边界”处理；供上层组装的中间结果由组装者统一复核。
+
 **完成条件：** 主模型是可检查的实际模型；每个 TCON 可追溯到路径、规则、分区、组合或迁移；粒度差异已拆开；第二模型如存在，新增覆盖明确；TCON 没有冒充可执行测试。
--- a/skills/designing-test-experiments/SKILL.md
+++ b/skills/designing-test-experiments/SKILL.md
@@ -92,4 +92,6 @@
 
 交付当前范围的用例、任务或章程及依据，分别说明设计完成情况、规则缺口和已核实的执行条件。下一步点名编写什么、准备什么或执行什么；未核实版本和环境时不能把用例草案称为可执行。调用方按已有请求决定持久化与执行。
 
+完整产物交付前，按 `challenging-test-designs` 的“调用与复核边界”处理；供上层组装的中间结果由组装者统一复核。
+
 **完成条件：** 所有本轮选中内容均已列入覆盖边界；常规 Case 可快速扫描，复杂 Case 有必要细节；每条 Case 状态诚实、前置独立、证据最小，并可追溯到候选点或 TCON；P0 与产品价值和核心证据一致；未执行内容没有冒充结果。
```
