# GPT-5.6 Agent交付自校修正试跑

日期：2026-09-08。候选为交付自校v3；执行者指定gpt-5.6-sol / medium、无父会话历史。主会话Agent独立读diff并给了两项修后反馈，执行者自行完成补正。用户没有被要求批准编号或文字修订。

这不是5.6单模型、无反馈一次成功的证明。主会话复核者与执行者不是同一上下文，主会话保留此前调查知识；这次观察的是Agent之间完成纠错闭环，没有证明换成另一位5.6审阅者也能取得同样结果。

## 输入和边界

输入为用户5.6产物的副本。允许修改两份副本；只依据产物记载规则与证据边界，未提供外部源码/Spec；不执行产品测试，不新增编号脚本，不向用户逐项确认文档纠错。

- 原TCO SHA256：96d93813725b5ee917c3243af0257918633cde75bb3bc0bbcae5dcf9119b7233
- 原测试分析 SHA256：05607de4bd09f69b74da10cdd14f6b65cfd754cfaf09bf6c0c23af1013e795a0
- 候选Skill SHA256：bd443abefa738c7eb7559d402ca4b013e86925f1db750e97a193a3727a545128

## 主Agent核对

| 检查 | 观察 |
| --- | --- |
| 悬空编号 | HIS-03映射到已有历史缺快照条件SNAP-07；SEC和LOG范围与实际定义对齐 |
| 状态主体 | 最终明确为产品类型＋外部编码的占用键；记录configId与键分开，旧记录保持已解绑，新记录可以占用 |
| 六格定位 | 模式矩阵与六个TCON各自能定位；未把模式矩阵完成称为产品执行通过 |
| 原规则保留 | 未把TCO的一般快照定义一律改成所有快照创建时固定，未撤销有效Spec口径 |
| 初次修改引入的回归 | 拆分六格后漏掉置信度/内部记录共同检查，部分行把解释文字写得像结果枚举；独立Agent指出后修正 |
| 再次修改中的边界遗漏 | TCO改写删掉明确的E2E禁止约束；独立Agent指出后恢复 |
| 用户负担 | 文档纠错与两项修后反馈均由Agent之间处理，未请求用户裁决 |
| 验证局限 | 单一样例；没有验证生产入口自动触发、其他业务保留样本或不需要修改的干净稿 |

因此，该样例支持“Agent能够完成发现、修正与补查”的方向；仍不能断言v3稳定优于原版，或一次自检就足以避免全部错误。是否已修改以文件diff为准，而非仅依执行者自述。

修改只在临时副本；用户下载原文件和正式Skill均未改动。原材料缺失、业务规则未知继续诚实保留。

## 执行Agent结果原文

# 交付自校结果

已修正两份草稿中的交付断裂：恢复 TCO 与需求分析副本之间的双向可用链接；明确本轮只交付 TCO 和候选测试条件，文中命令不构成执行授权；将风险表中的失效编号、缺失前缀和超出实际范围的引用对齐到现有 TCON；把 `TCON-WB-01～06` 拆为六个可独立定位的模式条件；明确配置状态模型的主体是全平台唯一占用键，并区分占用键状态、已解绑记录和历史快照观察；同步了在途任务中请求地址变更的未知范围，并把测试点末列改为“已有证据／安排”，避免误报为已生成用例。

共收到两项独立 Agent 反馈并完成修正：第一项补回审批六格共同观察的解释、置信度和内部记录，并把各行枚举判断明确写为 `auditResult`；第二项恢复项目地图中“保留 E2E 资产，但当前用户约束是不执行 E2E”的既有执行限制。本次修改后已复读对应观察点和执行边界。

剩余限制：本次交付自校未提供外部源码、原始 PRD、OpenSpec、真实协议或历史执行日志，因此不能独立验证草稿记录的实现事实、历史测试结果和当前 Revision；仍无法从现有文本确定的产品规则继续保留在“待确认与待取证”中，包括列表字段、在途任务恢复、Secret 风险接受、出站 URL 策略、`MANUAL_REVIEW` 外部协议值和上线数据规模。本次自校未执行产品测试，也未作发布判断。

## 可复核的副本差异

```diff
--- original/TCO.md
+++ revised/TCO.md
@@ -152,13 +152,13 @@
 | OpenSpec 自带脚本 | 特定 change 的隔离数据库验证 | 历史薄入口，需核对脚本与当前仓库规则 |
 | SIT/UAT | 真实配置与外部集成验收候选 | 当前无访问事实、账号、外部租户和凭证证据 |
 
-本项目保留 E2E 资产，但当前用户约束是不执行 E2E。所有执行必须只清理本任务创建的进程、测试库和容器，不停止 `maycur-ai-audit` 既有容器。
+本项目保留 E2E 资产，但当前用户约束是不执行 E2E。本轮只形成项目 TCO 和候选测试条件，不执行产品测试；表中的命令、环境和 SIT/UAT 都是后续取证入口，不表示本轮执行授权。若后续另行执行，仍不得停止 `maycur-ai-audit` 既有容器，并且只能清理对应测试任务创建的进程、测试库和容器。
 
 ## 需求测试资产索引
 
 | 需求 | 影响的业务范围／节点 | 分析 | 用例 | 依据（如有） |
 | --- | --- | --- | --- | --- |
-| 2026-09-05 租户内对接配置 | M-01、M-03、M-05、M-06、M-08；F-01/F-03/F-04/F-05 | [`2026-09-05-integration-config-test-analysis.md`](../openspec/changes/2026-09-05-integration-config/2026-09-05-integration-config-test-analysis.md) | 尚未建立独立用例文件；现有自动化见 change test | [`design.md`](../openspec/changes/2026-09-05-integration-config/design.md)、[`verify.md`](../openspec/changes/2026-09-05-integration-config/verify.md) |
+| 2026-09-05 租户内对接配置 | M-01、M-03、M-05、M-06、M-08；F-01/F-03/F-04/F-05 | [`2026-09-05-integration-config-test-analysis.md`](2026-09-05-integration-config-test-analysis.md) | 尚未建立独立用例文件；现有自动化见 change test | [`design.md`](../openspec/changes/2026-09-05-integration-config/design.md)、[`verify.md`](../openspec/changes/2026-09-05-integration-config/verify.md) |
 
 ## 项目级待办
 
--- original/2026-09-05-integration-config-test-analysis.md
+++ revised/2026-09-05-integration-config-test-analysis.md
@@ -2,9 +2,11 @@
 
 ## 任务与结论摘要
 
-本分析面向租户管理员维护外部连接、系统按明确连接取数、审核任务固定配置快照、来源系统回写及存量迁移这条完整业务链。需求来源是原始《AI审核平台 对接配置 PRD》，当前规则以本目录 [`design.md`](design.md) 记录的后续用户确认为准；项目关联见 [`testing/TCO.md`](../../../testing/TCO.md)。
+本分析面向租户管理员维护外部连接、系统按明确连接取数、审核任务固定配置快照、来源系统回写及存量迁移这条完整业务链。需求来源是原始《AI审核平台 对接配置 PRD》，当前规则以本目录 [`design.md`](design.md) 记录的后续用户确认为准；项目关联见同目录 [`TCO.md`](TCO.md)。
 
 当前阶段判定为**已实现且已有历史验证记录，尚未在本轮重新执行**。目标 feature 为 `f10bf001`，当前工作区是已集成并继续演进的 `release/20260910` `f856af46`。本轮读取了原 PRD、OpenSpec、feature 两个提交、当前实现、migration、变更测试与测试架构；没有执行测试或访问真实每刻环境，因此没有形成产品 Finding。
+
+本轮交付范围是项目 TCO 与候选测试条件，不生成可执行用例、不执行产品测试，也不作发布批准。本文出现的命令和环境动作都是后续取证入口，不表示本轮执行授权。
 
 结论：核心数据/API 设计对“租户自助、全平台唯一、当前连接精确读取、新任务快照、两种回写模式”已有较强的代码和自动化资产支持，但**现有证据不足以支持对当前 release 的完整验收主张**。最先应补的不是更多正常新增用例，而是：
 
@@ -21,7 +23,7 @@
 | --- | --- | --- | --- |
 | R-IC-01 存量快照 | 在隔离库中按 `prisma migrate deploy` → `integration:migrate --mode=DIRECT` 的真实顺序，预置旧旗舰版配置和无快照任务 | 迁移顺序观察、任务快照查询结果、可重复回归 | 不需要真实外部接口；本轮未获执行请求 |
 | R-IC-04 列表验收 | 产品/需求负责人确认请求地址、管理员工号是否仍是列表验收字段 | 当前有效 UI oracle；修正 PRD 或实现的明确决定 | 当前 PRD 与实现不一致 |
-| R-IC-02 在途任务 | 确认编辑凭证、改编码、解绑分别发生在任务创建后/回写前时的业务处理 | 三类事件的状态/恢复规则与测试模型 | 需求只明确历史展示和回写模式快照 |
+| R-IC-02 在途任务 | 确认编辑凭证、请求地址、外部编码或解绑分别发生在任务创建后/回写前时的业务处理 | 四类事件的状态/恢复规则与测试模型 | 需求只明确历史展示和回写模式快照 |
 | R-IC-03 安全边界 | 安全/产品负责人确认明文凭证风险接受、出站 URL 约束和日志要求 | 威胁模型、接受范围或控制要求 | 原 PRD 与后续实现口径冲突 |
 | 证据刷新 | 在待测精确 Revision 上重跑非 E2E 检查并保留机器可读摘要 | Revision、环境、命令、原始结果和清理证据 | 当前只存在历史文字记录 |
 | 长期回归 | 将唯一占用、快照不变、模式矩阵、迁移顺序提升到 regression | 默认 `pnpm test` 可持续保护的用例 | 当前主要位于 change test |
@@ -48,7 +50,7 @@
   W -->|按快照模式构造外部审批结果| D[(WebhookDelivery)]
   C -->|当前凭证与地址| D
   D --> E[Adapter 调用来源系统]
-  C -->|编辑或解绑| N[只影响后续任务和当前调用]
+  C -->|编辑或解绑| N[改变当前连接；在途回写行为待确认]
   S --> H[历史页面/既有任务语义]
 ```
 
@@ -65,33 +67,35 @@
 
 上述差异中，OpenSpec 只写“用户已确认”，未链接独立确认记录。本轮接受它作为当前项目规则，但建议需求负责人把安全和默认地址变更同步回权威 PRD，避免后续团队重新采用旧 oracle。
 
-### 配置生命周期模型
+### 配置唯一键占用模型
+
+模型主体是一个由“产品类型 + 外部租户编码”识别的全平台占用键；“当前占用”状态同时记录占用它的 `configId`。解绑会释放该键，但原 `configId` 记录仍保持已解绑；同键再次绑定时由新的 `configId` 占用，不是原记录复活。
 
 ```mermaid
 stateDiagram-v2
-  [*] --> 未配置
-  未配置 --> 当前有效: 保存成功且唯一键空闲
-  未配置 --> 未配置: 保存冲突/校验失败
-  当前有效 --> 当前有效: 编辑成功/新调用立即读新值
-  当前有效 --> 当前有效: 编辑冲突/原记录保持
-  当前有效 --> 已解绑: 二次确认并解绑
-  当前有效 --> 当前有效: 取消解绑
-  已解绑 --> 当前有效: 其他租户或本租户重新绑定同一键
-  已解绑 --> 已解绑: 历史任务继续读取旧快照
+  [*] --> 未占用
+  未占用 --> 当前占用: 保存成功/新 configId 占用
+  未占用 --> 未占用: 校验失败
+  当前占用 --> 当前占用: 编辑非唯一键字段成功
+  当前占用 --> 当前占用: 重复保存冲突/原占用保持
+  当前占用 --> 未占用: 二次确认并解绑/原 configId 标记 released
+  当前占用 --> 当前占用: 取消解绑
 ```
 
-覆盖准则：每个合法迁移、主要拒绝迁移、并发保存、跨租户动作和“当前记录/历史快照”两个观察位置都需要至少一个测试条件。编辑与解绑后的在途任务行为不在现有模型中有明确 oracle，保留为未知。
+覆盖准则：每个合法迁移、主要拒绝迁移、并发保存、跨租户动作和“当前占用记录/已解绑记录/历史快照”三个观察位置都需要至少一个测试条件。历史任务继续读取旧快照是解绑后的观察规则，不是占用键状态；同键重绑及新旧 `configId` 由 TCON-CFG-07 检查。修改产品或编码会同时释放旧键并占用新键，由 TCON-CFG-05～06 检查。编辑与解绑后的在途任务行为没有明确 oracle，保留为未知。
 
 ### 审批回写决策模型
 
-| 产品/阶段 | 快照模式 | AI 结果 | 对外 `approveResult` | 内部 `auditResult` / 最终事实 | 人工复核后来源回写 |
-| --- | --- | --- | --- | --- | --- |
-| 旗舰版 AI 阶段 | DIRECT | PASS | PASS | 保留 PASS | 不重复 |
-| 旗舰版 AI 阶段 | DIRECT | REJECT | REJECT | 保留 REJECT | 不重复 |
-| 旗舰版 AI 阶段 | DIRECT | MANUAL_REVIEW | MANUAL_REVIEW | 保留无法判断，人工结果另存 | 不重复 |
-| 旗舰版 AI 阶段 | PRE_REVIEW | PASS/REJECT/MANUAL_REVIEW | PASS | 保留真实 AI 结果 | 不重复 |
-| 国际版 | 固定直审语义 | PASS/REJECT | 最终结论 | 保留最终事实 | 按国际版既有链路 |
-| 非来源系统任务 | 无对应快照/来源 | 任意 | 不创建来源投递 | 内部事实照常保存 | 不适用 |
+| 模型行／测试点 | 产品/阶段 | 快照模式 | AI 结果 | 对外 `approveResult` | 内部 `auditResult` / 最终事实 | 人工复核后来源回写 |
+| --- | --- | --- | --- | --- | --- | --- |
+| TCON-WB-01 | 旗舰版 AI 阶段 | DIRECT | PASS | PASS | 保留 PASS | 不重复 |
+| TCON-WB-02 | 旗舰版 AI 阶段 | DIRECT | REJECT | REJECT | 保留 REJECT | 不重复 |
+| TCON-WB-03 | 旗舰版 AI 阶段 | DIRECT | MANUAL_REVIEW | MANUAL_REVIEW | 保留无法判断，人工结果另存 | 不重复 |
+| TCON-WB-04 | 旗舰版 AI 阶段 | PRE_REVIEW | PASS | PASS | 保留 PASS | 不重复 |
+| TCON-WB-05 | 旗舰版 AI 阶段 | PRE_REVIEW | REJECT | PASS | 保留 REJECT | 不重复 |
+| TCON-WB-06 | 旗舰版 AI 阶段 | PRE_REVIEW | MANUAL_REVIEW | PASS | 保留无法判断，人工结果另存 | 不重复 |
+| TCON-WB-09 | 国际版 | 固定直审语义 | PASS/REJECT | 最终结论 | 保留最终事实 | 按国际版既有链路 |
+| TCON-WB-12 | 非来源系统任务 | 无对应快照/来源 | 任意 | 不创建来源投递 | 内部事实照常保存 | 不适用 |
 
 现有变更测试覆盖了旗舰版两种模式与三种 AI 结果的六格组合。仍需核对真实每刻协议是否接受 `MANUAL_REVIEW` 作为直审审批结果，以及首次投递失败、重试和人工复核并发时“不重复”的判断依据。
 
@@ -115,14 +119,14 @@
 
 | ID | 场景与业务影响 | 判断及依据 | 状态 | 关联测试 | 最新证据与变化 | 下一步 |
 | --- | --- | --- | --- | --- | --- | --- |
-| R-IC-01 | 首次上线后旗舰版存量任务没有配置快照；历史页面显示“配置缺失”，在人工复核/回写兼容路径上可能采用当前配置或产生额外投递 | 威胁“存量平滑迁移、历史可追溯”；SQL 先填地址，脚本只扫空地址，发生机制有直接静态证据；影响范围取决于上线时存量任务量 | 待调查（当前最高） | TCON-MIG-01、02、HIS-03 | 现有测试没有按真实顺序预置旧数据 | 隔离库复现实序；若成立，修正迁移选择条件并增加回归 |
-| R-IC-02 | 任务创建后、回写前编辑凭证/编码或解绑；快照保留模式和身份但没有凭证，Adapter 只读当前未解绑配置，可能使既有任务无法回写 | 威胁核心闭环；安全上不应快照 Secret，但业务恢复规则未知 | 待调查 | TCON-SNAP-05、06、WB-10 | 源码确认回写按快照外部编码 + 当前连接凭证组合 | 产品确认三种事件的期望；再设计恢复/重试测试 |
-| R-IC-03 | 明文 Secret、可回显 Secret 或任意 HTTP/HTTPS requestUrl 被滥用，可能泄露外部凭证或形成服务端请求伪造 | 后续规则允许明文存储，但原 PRD 明确要求加密/脱敏；URL 校验只限制协议和 URL 内用户名密码，未见主机/私网控制 | 待调查（独立安全专题） | TCON-SEC-01～06 | 当前 API 和 UI 需要返回真实 Secret；连通性测试会从服务端发起请求 | 取得安全风险接受/控制边界；验证日志、备份、出站网络与授权 |
-| R-IC-04 | 管理员无法从列表直接辨识请求地址和管理员工号，可能编辑错误连接或无法快速排障 | 原 PRD 明确列为列表字段；当前 UI 只显示产品、企业名称、企业编码和操作，现有测试未覆盖 | 待调查 | TCON-UI-06、07 | 静态 UI 对照发现差异；后续 OpenSpec 未明确取消这两列 | 产品确认字段是否仍必需；按决定修文档或实现 |
+| R-IC-01 | 首次上线后旗舰版存量任务没有配置快照；历史页面显示“配置缺失”，在人工复核/回写兼容路径上可能采用当前配置或产生额外投递 | 威胁“存量平滑迁移、历史可追溯”；SQL 先填地址，脚本只扫空地址，发生机制有直接静态证据；影响范围取决于上线时存量任务量 | 待调查（当前最高） | TCON-MIG-01、TCON-MIG-02、TCON-SNAP-07 | 现有测试没有按真实顺序预置旧数据 | 隔离库复现实序；若成立，修正迁移选择条件并增加回归 |
+| R-IC-02 | 任务创建后、回写前编辑凭证、请求地址、外部编码或解绑；快照保留模式和身份但没有凭证，Adapter 只读当前未解绑配置，可能使既有任务无法回写 | 威胁核心闭环；安全上不应快照 Secret，但业务恢复规则未知 | 待调查 | TCON-SNAP-05、TCON-SNAP-06、TCON-WB-10 | 源码确认回写按快照外部编码 + 当前连接凭证组合 | 产品确认四类事件的期望；再设计恢复/重试测试 |
+| R-IC-03 | 明文 Secret、可回显 Secret 或任意 HTTP/HTTPS requestUrl 被滥用，可能泄露外部凭证或形成服务端请求伪造 | 后续规则允许明文存储，但原 PRD 明确要求加密/脱敏；URL 校验只限制协议和 URL 内用户名密码，未见主机/私网控制 | 待调查（独立安全专题） | TCON-SEC-01～04、TCON-LOG-01～02 | 当前 API 和 UI 需要返回真实 Secret；连通性测试会从服务端发起请求 | 取得安全风险接受/控制边界；验证日志、备份、出站网络与授权 |
+| R-IC-04 | 管理员无法从列表直接辨识请求地址和管理员工号，可能编辑错误连接或无法快速排障 | 原 PRD 明确列为列表字段；当前 UI 只显示产品、企业名称、企业编码和操作，现有测试未覆盖 | 待调查 | TCON-UI-06、TCON-UI-07 | 静态 UI 对照发现差异；后续 OpenSpec 未明确取消这两列 | 产品确认字段是否仍必需；按决定修文档或实现 |
 | R-IC-05 | 某个元数据、测试任务、公开场景、Tool、旗舰版/国际版取数或回写消费者仍使用模糊/旧配置源 | 配置统一是系统目标，消费者多且跨 Web/Worker/Adapter；统一函数存在，部分旧测试已适配 | 处理中 | TCON-READ-01～10 | 当前代码多数按 tenant/source/code 精确查询；未取得全消费者动态证据 | 建立消费者追溯矩阵并在多连接数据下做集成验证 |
 | R-IC-06 | 历史验证与当前代码不一致，团队误把文字通过率当成当前 release 证据 | `verify.md` 未记录精确被测 SHA；feature 后又有相关变更；原始日志未提交；当前测试文件也已继续变化 | 待调查 | TCON-EVD-01～03 | 当前 HEAD `f856af46`，feature `f10bf001`，后续有 `0bfb1fee` 等相关变更 | 在目标 SHA 重跑并记录 Revision、环境、原始摘要和清理证据 |
-| R-IC-07 | 受控 HTTP 替身通过，但真实旗舰版/国际版认证、地址、错误码或回写 payload 不兼容 | 当前证据明确没有调用真实费控接口；影响连通性和最终审批 | 待调查 | TCON-CONN-07、WB-08、09 | 只有受控 Response 和既有国际版回归 | 在受控 SIT/UAT 租户做协议级验证，失败不触碰生产数据 |
-| R-IC-08 | 日志包含 Secret 或缺少足够上下文，泄漏凭证或无法排障 | PRD 明确禁止记录明文 Secret，项目又要求外部调用记录请求/响应；两者需要字段级 oracle | 待调查 | TCON-LOG-01～04 | 配置日志上下文未带 Secret；外部客户端日志需按实际 payload 复核 | 捕获测试日志，断言 Secret 不出现且 tenant/config/request 标识完整 |
+| R-IC-07 | 受控 HTTP 替身通过，但真实旗舰版/国际版认证、地址、错误码或回写 payload 不兼容 | 当前证据明确没有调用真实费控接口；影响连通性和最终审批 | 待调查 | TCON-CONN-07、TCON-WB-08、TCON-WB-09 | 只有受控 Response 和既有国际版回归 | 在受控 SIT/UAT 租户做协议级验证，失败不触碰生产数据 |
+| R-IC-08 | 日志包含 Secret 或缺少足够上下文，泄漏凭证或无法排障 | PRD 明确禁止记录明文 Secret，项目又要求外部调用记录请求/响应；两者需要字段级 oracle | 待调查 | TCON-LOG-01～02 | 配置日志上下文未带 Secret；外部客户端日志需按实际 payload 复核 | 捕获测试日志，断言 Secret 不出现且 tenant/config/request 标识完整 |
 
 ## 完整测试点清单
 
@@ -164,7 +168,7 @@
 
 ### 权限、页面与操作反馈
 
-| 测试点 | 场景／条件 | 检查重点 | 关联用例 |
+| 测试点 | 场景／条件 | 检查重点 | 已有证据／安排 |
 | --- | --- | --- | --- |
 | TCON-UI-01 租户管理员进入模块 | TENANT_ADMIN 直接打开页面 | 菜单位于管理后台且在人员管理上方；页面/API 可用 | 现有 change test 仅覆盖 API |
 | TCON-UI-02 审核员不可见不可调 | REVIEWER 看菜单并直接调用 GET/POST/PATCH/DELETE/test | 菜单隐藏；所有管理动作拒绝且不泄漏数据 | 现有 API 部分覆盖；页面后续 |
@@ -178,7 +182,7 @@
 
 ### 保存、编辑、唯一占用与解绑
 
-| 测试点 | 场景／条件 | 检查重点 | 关联用例 |
+| 测试点 | 场景／条件 | 检查重点 | 已有证据／安排 |
 | --- | --- | --- | --- |
 | TCON-CFG-01 新增完整旗舰版配置 | 必填齐全，管理员工号为空/有值 | 保存成功，值和租户归属正确；不隐式调用连通性测试 | 现有覆盖 |
 | TCON-CFG-02 国际版配置 | 相同企业编码但不同产品 | 允许共存；国际版沿用直审语义和自身字段 | 现有部分覆盖 |
@@ -192,7 +196,7 @@
 
 ### 独立连通性测试
 
-| 测试点 | 场景／条件 | 检查重点 | 关联用例 |
+| 测试点 | 场景／条件 | 检查重点 | 已有证据／安排 |
 | --- | --- | --- | --- |
 | TCON-CONN-01 旗舰版认证成功 | token 与 entCode 匹配 | 提示通过，不保存配置、不占用编码 | 现有覆盖 |
 | TCON-CONN-02 企业编码不匹配 | 认证成功但 entCode 不同 | 明确提示编码问题；同参数仍可保存 | 现有覆盖 |
@@ -204,7 +208,7 @@
 
 ### 当前配置读取与跨模块消费者
 
-| 测试点 | 场景／条件 | 检查重点 | 关联用例 |
+| 测试点 | 场景／条件 | 检查重点 | 已有证据／安排 |
 | --- | --- | --- | --- |
 | TCON-READ-01 精确选择 | 同租户同产品两配置、另一租户同产品配置 | tenant+source+code 唯一命中；缺任一身份不取第一条 | 现有查询实现；需消费者集成 |
 | TCON-READ-02 当前无配置/已解绑 | 找不到或记录 released | 返回“未配置对接信息”或模块对应友好失败，不用旧全局配置 | 部分覆盖 |
@@ -219,7 +223,7 @@
 
 ### 快照、历史展示和在途任务
 
-| 测试点 | 场景／条件 | 检查重点 | 关联用例 |
+| 测试点 | 场景／条件 | 检查重点 | 已有证据／安排 |
 | --- | --- | --- | --- |
 | TCON-SNAP-01 新任务固定快照 | Adapter 受理时配置有效 | configId、产品、企业名/编码、地址、模式、时间完整，无 Key/Secret | 现有覆盖 configId/无 Secret，需字段完整性 |
 | TCON-SNAP-02 多入口一致 | Web、OpenAPI、旗舰版、国际版入站 | 需要连接的入口均在统一创建边界固定；无连接业务不伪造 | 后续 |
@@ -232,9 +236,16 @@
 
 ### 审批回写、人工复核与恢复
 
-| 测试点 | 场景／条件 | 检查重点 | 关联用例 |
-| --- | --- | --- | --- |
-| TCON-WB-01～06 模式矩阵 | DIRECT/PRE_REVIEW × PASS/REJECT/MANUAL_REVIEW | approveResult 按表；auditResult、解释、置信度和内部记录不被预审覆盖 | 现有六格覆盖 payload 核心字段 |
+TCON-WB-01～06 共同检查：`auditResult` 保留该格的真实 AI 结果；解释、置信度和内部记录不因 PRE_REVIEW 对外统一为 PASS 而被覆盖。各行只列该格额外需要区分的对外结果。
+
+| 测试点 | 场景／条件 | 检查重点 | 已有证据／安排 |
+| --- | --- | --- | --- |
+| TCON-WB-01 DIRECT + PASS | 旗舰版、任务快照模式为 DIRECT、AI 结果为 PASS | approveResult 为 PASS；内部 `auditResult` 保持 PASS | 现有参数化资产覆盖 payload 核心字段 |
+| TCON-WB-02 DIRECT + REJECT | 旗舰版、任务快照模式为 DIRECT、AI 结果为 REJECT | approveResult 为 REJECT；内部 `auditResult` 保持 REJECT | 现有参数化资产覆盖 payload 核心字段 |
+| TCON-WB-03 DIRECT + MANUAL_REVIEW | 旗舰版、任务快照模式为 DIRECT、AI 结果为 MANUAL_REVIEW | approveResult 为 MANUAL_REVIEW；内部 `auditResult` 保留 MANUAL_REVIEW／无法判断语义 | 现有参数化资产覆盖 payload 核心字段；真实协议值待取证 |
+| TCON-WB-04 PRE_REVIEW + PASS | 旗舰版、任务快照模式为 PRE_REVIEW、AI 结果为 PASS | approveResult 为 PASS；内部 `auditResult` 保持 PASS | 现有参数化资产覆盖 payload 核心字段 |
+| TCON-WB-05 PRE_REVIEW + REJECT | 旗舰版、任务快照模式为 PRE_REVIEW、AI 结果为 REJECT | approveResult 为 PASS；内部 `auditResult` 仍为 REJECT | 现有参数化资产覆盖 payload 核心字段 |
+| TCON-WB-06 PRE_REVIEW + MANUAL_REVIEW | 旗舰版、任务快照模式为 PRE_REVIEW、AI 结果为 MANUAL_REVIEW | approveResult 为 PASS；内部 `auditResult` 保留 MANUAL_REVIEW／无法判断语义 | 现有参数化资产覆盖 payload 核心字段 |
 | TCON-WB-07 模式修改后旧/新任务 | 保存模式前后分别创建任务 | 旧任务用旧模式，新任务用新模式 | 现有旧任务覆盖，需新任务对照 |
 | TCON-WB-08 旗舰版真实 payload | taskId、formDataCode、公开详情 URL、理由 | 协议字段和值与外部系统权威契约一致 | 受控替身覆盖；真实契约待取证 |
 | TCON-WB-09 国际版保持既有行为 | PASS/REJECT、人工复核、失败 | 不受旗舰版模式影响，使用国际版连接 | 既有回归部分覆盖 |
@@ -244,7 +255,7 @@
 
 ### 存量迁移与发布顺序
 
-| 测试点 | 场景／条件 | 检查重点 | 关联用例 |
+| 测试点 | 场景／条件 | 检查重点 | 已有证据／安排 |
 | --- | --- | --- | --- |
 | TCON-MIG-01 真实完整顺序 | migration 前已存在旗舰版配置和无快照任务 | deploy 后再运行脚本，连接与所有可恢复任务都获得预期初始化 | 本轮首要 probe；现有未覆盖 |
 | TCON-MIG-02 国际版顺序 | migration 前已有国际版记录 | 使用环境地址、直审模式和历史快照，旗舰/国际互不污染 | 现有未覆盖完整顺序 |
@@ -258,7 +269,7 @@
 
 ### 安全、日志、兼容与证据
 
-| 测试点 | 场景／条件 | 检查重点 | 关联用例 |
+| 测试点 | 场景／条件 | 检查重点 | 已有证据／安排 |
 | --- | --- | --- | --- |
 | TCON-SEC-01 凭证落库 | 新增/编辑/迁移 | 当前规则下真实值与标志符合设计，同时记录风险接受范围 | 现有覆盖行为，不等于安全接受 |
 | TCON-SEC-02 接口凭证暴露 | tenant admin/platform admin/reviewer/未登录调用列表与错误路径 | 只有授权角色得到设计允许的字段；缓存、错误和跨租户路径不泄漏 | 权限部分覆盖 |
@@ -309,7 +320,7 @@
 | 问题 | 影响的测试 | 取证来源或确认人 | 下一动作 |
 | --- | --- | --- | --- |
 | 配置列表是否仍必须展示请求地址和管理员工号？ | UI 字段、长值、排障效率、验收结论 | 产品/需求负责人；原 PRD 2.3 | 确认后修文档或实现，并补组件/人工验证 |
-| 在途任务遇到凭证编辑、编码修改或解绑时，各自应如何回写与恢复？ | 快照边界、回写、重试、解绑、错误提示 | 产品 + 集成研发/运维 | 形成三个事件的状态规则，再派生用例 |
+| 在途任务遇到凭证编辑、请求地址修改、外部编码修改或解绑时，各自应如何回写与恢复？ | 快照边界、回写、重试、解绑、错误提示 | 产品 + 集成研发/运维 | 形成四类事件的状态规则，再派生用例 |
 | 明文存储和授权角色可查看真实 Secret 是否已经得到安全风险接受？ | 数据库、API、UI、日志、备份、权限 | 安全/产品实际负责人 | 记录接受范围或增加控制，不用当前实现反推已接受 |
 | requestUrl 是否允许私网、localhost、重定向和纯 HTTP？ | 连通性测试及所有 Adapter 调用 | 安全/运维/架构 | 定义出站策略和可验证 oracle |
 | 直审的 `MANUAL_REVIEW` 是否是每刻接口接受的审批值？ | 回写决策矩阵与真实协议 | 外部协议负责人/接口契约 | 取得契约样本或受控环境观察 |
@@ -317,7 +328,7 @@
 
 ## 关联产物与依据
 
-- 项目地图：[`testing/TCO.md`](../../../testing/TCO.md)
+- 项目地图：[`TCO.md`](TCO.md)
 - 当前规则：[`design.md`](design.md)
 - 部署与迁移说明：[`deployment.md`](deployment.md)
 - 历史验证记录：[`verify.md`](verify.md)
```
